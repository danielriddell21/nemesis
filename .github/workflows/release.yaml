name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    uses: danielriddell21/github-actions/.github/workflows/release.yaml@v2
    permissions:
      contents: write
    with:
      cask-name: nemesis
      cask-desc: A procedurally generated first-person stealth game hunted by an Alien-Isolation-style AI
      cask-caveats: >-
        nemesis opens an Ebiten window and is macOS-only; on Linux/Windows,
        build from source.
    secrets:
      tap-app-private-key: ${{ secrets.TAP_APP_PRIVATE_KEY }}
